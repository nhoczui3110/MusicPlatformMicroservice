package com.devteria.profile.mapper;

import com.devteria.profile.entity.Follows;
import org.mapstruct.Mapper;

@Mapper(componentModel = "spring")
public interface FollowsMapper {
//    Follows toFollows(Foll)
}
